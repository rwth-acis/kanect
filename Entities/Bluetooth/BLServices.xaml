<Window x:Class="KonnectUI.Entities.Bluetooth.BLServices"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KonnectUI.Entities.Bluetooth"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="Bluetooth Services" Height="180" Width="300"
        >
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="105*" />
            <ColumnDefinition Width="41*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2">Service UUID:</Label>
        <TextBox Name="txtUUID" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"></TextBox>
        <Button Name="btnConnect" Content="Connect" Grid.Column="0" Grid.Row="3" Click="btnConnect_Click" />
    </Grid>
</Window>
